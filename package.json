{
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm --workspace bluos run build",
    "start": "npm --workspace bluos run start",
    "start:nodemon": "npm --workspace bluos run start:nodemon",
    "start:typescript": "npm --workspace bluos run start:typescript"
  }
}
